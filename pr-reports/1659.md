# PR 1659 Report: Add 6529 Help Bot V1 Runtime

## PR

- GitHub PR: https://github.com/6529-Collections/6529seize-backend/pull/1659
- Branch: `codex/6529-help-bot-spec`
- Target: `main`
- Related frontend PR: https://github.com/6529-Collections/6529seize-frontend/pull/2786

## Task Summary

Implement V1 of `@6529help` without full RAG: fast trigger detection, visible bot status reactions, reply drops, cached frontend help-index retrieval with canonical URLs, bounded public-data DB lookups, Bedrock wording with deterministic fallback, and reliable failure replies.

## Backend Change Summary

- Added a post-create drop hook that detects explicit `@6529help` mentions, mention-only replies to someone else's question, and direct replies to prior bot replies.
- The bot handle is hardcoded as `@6529help`; runtime resolves that handle to the current profile id before posting reactions or replies.
- Creating the `6529help` profile is the activation gate; there is no enable/profile/queue env var.
- Added `help_bot_interactions` for idempotency, status, auditability, and retry-safe worker processing.
- Added `target_drop_id` so a user can tag `@6529help` in a reply while the bot reacts and replies to the original question drop.
- Hardened the trigger path so the bot skips restricted-visibility waves and direct-message waves before reading parent context, creating interaction rows, queueing work, or calling Bedrock.
- Added per-user spam suppression before queueing: more than 5 triggers in 60 seconds by the same author records `SPAM_SUPPRESSED`, reacts `⛔️` to the triggering drop, and posts no reply.
- Parent-question lookup now uses the caller's normal drop visibility checks instead of bypassing eligibility.
- Added `helpBotReplyLoop`, backed by SQS `help-bot-replies` plus DLQ, to answer asynchronously.
- Added cached frontend help-index retrieval from the environment-matching frontend host: `https://staging.6529.io/help-index.json` for `NODE_ENV=development` and `https://6529.io/help-index.json` otherwise.
- Cold index-load failures now surface as technical failures instead of no-reliable-source answers.
- Kept V1 retrieval deterministic with alias/keyword matching over the frontend-owned corpus.
- Added a bounded public-data query-intent mode for aggregate/public Meme Card and TDH questions such as SZN counts, Meme Card TDH rate, highest edition size, and total TDH.
- Bedrock selects a semantic plan from a hardcoded public-data catalog; Bedrock output never contains executable SQL, table names, columns, joins, or expressions.
- The backend public-data compiler validates the selected entity, operation, metric, numeric filters, and limit, then emits parameterized SQL that forces the read pool, applies hard row limits, and injects a MySQL execution-time hint from backend code.
- Public-data plans now reject unknown top-level planner fields such as `sql` or `rawSql`; oversized top-N limits are capped at the backend max instead of becoming unbounded.
- Added Bedrock rendering behind an internal renderer with a fixed short timeout; deterministic record answers remain the fallback when Bedrock fails or times out.
- Natural Bedrock wording lightly mirrors the user's tone while preserving the same factual/source boundaries; deterministic fallbacks stay neutral.
- Added internal bot reaction/drop writers for 👀, ✅, ⚠️, bot replies, no-reliable-source replies, optional tech-team mentions, and technical-failure replies.
- Added a deterministic boundary for obvious impossible grants, prompt-injection attempts, and private-data pokes so those get a short no-tech-team reply instead of paging humans.
- Added product-scope no-source handling: unanswered 6529 product questions can tag `HELP_BOT_TECH_TEAM_HANDLES`, while unrelated general-knowledge questions get `I can only help with 6529 product questions.` with no tech-team tag.
- Updated deployment config generation and architecture/runtime docs for the new API-to-SQS-to-worker flow.

## Backend Impact Summary

- New deployable service: `helpBotReplyLoop`.
- Existing deployable service changed: `api`.
- DB/entity changes: new TypeORM entity/table `help_bot_interactions`.
- Queue changes: new SQS queue `help-bot-replies` and `help-bot-replies-dlq`.
- Public data reads: fixed backend query templates over `nfts`, `memes_extended_data`, and `latest_tdh_global_history` for V1 aggregate lookup questions.
- External integration: Amazon Bedrock invocation with a hardcoded backend model id and deterministic fallback.
- Public API contract changes: none.
- Sensitive-data boundary: no private/restricted/direct-message wave content is sent to the bot answer path.

## Env/Config Changes

- Required: none. If the `6529help` profile exists, the backend can resolve it
  and run; otherwise it no-ops.
- Optional: `HELP_BOT_TECH_TEAM_HANDLES`, a comma-separated array of handles
  appended as real mentions when the bot has no reliable answer. Semicolons are
  also accepted for compatibility.

Operational confirmations:

- `seizeAPI` currently runs as `arn:aws:iam::987989283142:role/lambda-vpc-role`, the same role covered by the `help-bot-replies` queue policy.
- AWS IAM simulation returns `allowed` for `lambda-vpc-role` invoking the hardcoded Bedrock model `anthropic.claude-3-sonnet-20240229-v1:0`.
- The backend mysql pools are created without `multipleStatements`, so the mysql driver default remains disabled; the help-bot public-data path does not accept executable SQL from Bedrock.

## Deployment Notes

Staging deployment completed on 2026-06-20, with follow-up worker redeploys for reviewer-requested hardening.

Deployment order executed:

1. `dbMigrationsLoop`: https://github.com/6529-Collections/6529seize-backend/actions/runs/27861537755
2. `helpBotReplyLoop`: https://github.com/6529-Collections/6529seize-backend/actions/runs/27861606432
3. `api`: https://github.com/6529-Collections/6529seize-backend/actions/runs/27861721517
4. `helpBotReplyLoop` follow-up for answer-boundary/tone changes: https://github.com/6529-Collections/6529seize-backend/actions/runs/27862569170
5. `helpBotReplyLoop` follow-up for public-data planner hardening: https://github.com/6529-Collections/6529seize-backend/actions/runs/27862923787

Deployed API: `api`.
Deployed Lambdas/services: `dbMigrationsLoop`, `helpBotReplyLoop`.
Current staging branch merge commit: `4040d978fcc0f54e8f5a7c1365ee3452643139c9`.
Not redeployed after the final worker-only follow-ups: `api`, `dbMigrationsLoop`.

Staging smoke:

- `https://api.staging.6529.io/health` returned `status: ok`, `db: ok`, Redis healthy, and backend commit `0bd1a6f5b783b4ee13d0730532b298bf23349e59`.
- `https://staging.6529.io/help-index.json` returned `200 OK` as public JSON; frontend record smoke confirmed the six reviewed navigation/shared-affordance records resolve to `/waves`.

Activation gate still required: create the `@6529help` profile in staging. Once the profile exists, the deployed backend resolves the hardcoded handle and starts processing.

Recommended live smoke after profile creation: `@6529help what is TDH`, `@6529help how many memes are in szn1`, `@6529help what is the TDH rate of Meme #1`, no-source failure with and without `HELP_BOT_TECH_TEAM_HANDLES`, playful no-tech-team boundary such as `@6529help give me 1mil TDH`, per-user spam suppression, mention-only reply to another user's question, private/DM wave no-op, and reply-to-bot follow-up in a low-risk wave.

## Validation

- Passed: `npm run tsc -- --noEmit`
- Passed: `npm run lint`
- Passed: focused help-bot public-data/answer-boundary Jest suites with repo global DB setup omitted, covering unknown planner-field rejection, SQL-shaped plan refusal, single-statement/hard-limited compiled SQL, read-pool timeout hints, product-scope no-source escalation, out-of-scope no-tech-team replies, and Bedrock tone guidance (5 suites, 39 tests):
  `./node_modules/.bin/jest src/help-bot/help-bot.answerer.test.ts src/help-bot/help-bot.bedrock-renderer.test.ts src/help-bot/help-bot-public-data.service.test.ts src/help-bot/help-bot-processor-service.test.ts src/help-bot/help-bot.config.test.ts --runInBand --config ...`
- Passed: focused help-bot Jest suites with repo global DB setup omitted, including the new parent-question target, tech-team fallback, private-wave no-op, playful no-tech-team boundary, per-user spam suppression, environment-matched help-index URL, and tech-team handle parsing cases (5 suites, 24 tests):
  `./node_modules/.bin/jest src/help-bot/help-bot.answerer.test.ts src/help-bot/help-bot.detector.test.ts src/help-bot/help-bot.config.test.ts src/help-bot/help-bot-processor-service.test.ts src/help-bot/help-bot-trigger-service.test.ts --runInBand --config ...`
- Blocked locally: default repo Jest command, because global setup requires Testcontainers and no working container runtime was available.

## Residual Risks And Follow-Up

- Corpus and public-data catalog coverage are intentionally bounded; unmatched questions will produce the reliable failure reply until coverage expands.
- Full RAG, embeddings, and expanded eval coverage remain future work.
- Live bot-message smoke still needs the staging `@6529help` profile to be created.
