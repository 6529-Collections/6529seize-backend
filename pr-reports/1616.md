# PR Report: Wallet auth session v2 APIs and transfer codes

- GitHub PR: https://github.com/6529-Collections/6529seize-backend/pull/1616
- Branch: `codex/wallet-security-hardening`
- Target: `main`
- Related frontend PR: https://github.com/6529-Collections/6529seize-frontend/pull/2556

## Task Summary

Auth v2 backend hardening for wallet sessions, structured wallet signatures, connection sharing, websocket authentication compatibility, and backend-owned auth rollout settings.

## Backend Change Summary

- Added/updated auth session v2 API behavior for web/native wallet sessions, refresh/logout, nonce/signature handling, and connection transfer/redeem flows.
- Added database-backed wallet auth session and connection transfer persistence.
- Hardened structured wallet signature verification, replay protection, allowed-domain/audience checks, and websocket authentication expiry handling.
- Added `/api/settings.auth` rollout settings so web can learn backend strict-mode and migration-deadline state without redeploying frontend.
- Bound legacy refresh-token role refreshes server-side so v1 refresh preserves the originally validated role instead of trusting a new client-selected role.
- Added `AUTH_LEGACY_REFRESH_DISABLED` as a backend-only shutdown flag for `/auth/redeem-refresh-token`; when true, the endpoint returns `410 Gone` without removing the route.
- Added credentialed CORS allowlisting for v2 web cookie auth while keeping the general public API CORS wildcard/non-credentialed for external API clients.
- Added the wallet auth session v2 migration plan in `docs/auth/session-v2-migration-plan.md`.
- Updated OpenAPI/auth contract surfaces and generated API artifacts where applicable.
- Added and iterated focused auth/session/signature tests.

## Backend Impact Summary

- New Lambdas/services created: `None`
- Existing Lambdas/services changed: `api`
- Existing Lambdas/services redeployed due to shared-code impact: `api`
- API changes: auth session v2 routes, nonce/signature behavior, connection transfer/redeem behavior, websocket auth handling
- DB/entity/migration changes: wallet auth session and wallet connection transfer persistence/entity sync; nullable `refresh_tokens.role` for legacy role binding
- Queue/event/media/edge/integration changes: `None`
- Backend docs/architecture docs: no public architecture-doc update was identified as required for this report-only update

## Env And Config

| Name                                     | Owner                                | Status                               | Requiredness                                                       | Purpose                                                                                                                                        | Default/Fallback                           | Recommended staging value shape                                                                                                            | Location                      | Rollout / failure notes                                                                                                                                                                                                                            |
| ---------------------------------------- | ------------------------------------ | ------------------------------------ | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AUTH_SESSION_HASH_SECRET`               | Backend API                          | New/recommended                      | Strongly recommended                                               | Dedicated secret for hashing session and refresh token material                                                                                | Falls back to existing JWT secret if unset | Dedicated high-entropy secret, different from JWT secret                                                                                   | Secret manager/SSM/Lambda env | If not set, auth still works, but session hash isolation is reduced; rotating JWT secret would also affect session hashing.                                                                                                                        |
| `AUTH_CONNECTION_SHARING_DISABLED`       | Backend API                          | Optional kill switch                 | Optional; set only to disable connection sharing                   | Disables web-to-native connection transfer/redeem endpoints when explicitly true                                                               | Enabled unless explicitly `true`           | Leave unset/`false` for normal rollout; set `true` only when connection sharing must be disabled                                           | Lambda/API env or SSM         | If not set/false, connection sharing endpoints are available. If true, normal auth still works but connection sharing endpoints remain unavailable.                                                                                                |
| `AUTH_LEGACY_REFRESH_DISABLED`           | Backend API                          | Final v1 shutdown flag               | Optional; set only after migration grace period                    | Disables `/auth/redeem-refresh-token` while leaving a deliberate endpoint response                                                             | Enabled unless explicitly `true`           | Leave unset/`false` through silent rollout and grace period; set `true` only when v1 refresh should stop                                   | Lambda/API env or SSM         | If true, legacy refresh token holders receive `410 Gone` and must re-authenticate through v2. This is separate from `AUTH_STRUCTURED_SIGNATURES_REQUIRED`, which blocks legacy signing but not existing refresh tokens.                            |
| `AUTH_CONNECTION_SHARE_CODE_TTL_SECONDS` | Backend API                          | Optional                             | Optional                                                           | Controls connection transfer code lifetime                                                                                                     | Defaults to about `300` seconds            | Positive integer, normally `300`                                                                                                           | Lambda/API env                | If not set, default TTL is used; too low causes handoff expiry, too high increases code exposure window.                                                                                                                                           |
| `AUTH_WEB_CREDENTIAL_ORIGINS`            | Backend API                          | Required for web v2                  | Required before FE v2 web rollout                                  | Exact browser origins allowed to use credentialed CORS on v2 web-cookie auth routes                                                            | Empty list                                 | Production: `https://6529.io`; staging: `https://staging.6529.io`                                                                          | Lambda/API env                | Put web origins here, not API origins. If missing/wrong, browser v2 session nonce/login/refresh/logout will fail while public non-credentialed API CORS remains open for external clients.                                                         |
| `AUTH_WALLET_CHAIN_ID`                   | Backend API                          | Optional                             | Optional                                                           | Pins structured wallet-auth verification to the configured auth chain                                                                          | `1`                                        | `1` unless intentionally testing another supported auth chain                                                                              | Lambda/API env                | Invalid or non-positive values throw. This prevents clients from choosing the accepted auth chain through the nonce request shape.                                                                                                                 |
| `AUTH_STRUCTURED_SIGNATURES_REQUIRED`    | Backend API and `/api/settings.auth` | Rollout/enforcement flag             | Optional initially; final strict-mode flag                         | Rejects legacy/unstructured signatures where structured signatures are expected, and is exposed to FE as `auth.structured_signatures_required` | `false` when unset                         | Keep `false` for silent/backend-first deploy and migration-deadline rollout; set `true` only after FE/mobile/external clients are verified | Lambda/API env                | If not set/false, backend stays backwards-compatible and FE uses the deadline if one is set. If true too early, old clients and unstructured-signature flows can fail.                                                                             |
| `SESSION_V2_MIGRATION_DEADLINE`          | Backend API and `/api/settings.auth` | New rollout setting                  | Optional; set after FE deploy when migration campaign should begin | Exposes `auth.session_v2_migration_deadline` for FE v1-auth migration prompts and deadline capping                                             | `null` when unset                          | ISO datetime with timezone, for example `2026-06-25T00:00:00.000Z`                                                                         | Lambda/API env                | If not set, FE sees no migration deadline and stays silent unless strict mode is true. If invalid, settings startup/request path throws. If in the past, migrated FE will kick out v1 sessions on validation.                                      |
| `AUTH_SIGNATURE_ALLOWED_DOMAINS`         | Backend API                          | Optional/required by domain coverage | Required if staging domains are not already covered                | Explicit allowed signing domains                                                                                                               | Repo/service default list                  | Comma-separated first-party staging domains                                                                                                | Lambda/API env or SSM         | If missing and defaults do not cover the web domain, valid structured signatures are rejected. Over-broad values weaken domain binding.                                                                                                            |
| `AUTH_SIGNATURE_ALLOWED_DOMAIN_SUFFIXES` | Backend API                          | Optional/required by domain coverage | Required if suffix coverage is needed                              | Allows first-party domain suffixes                                                                                                             | Repo/service default list                  | Comma-separated first-party suffixes                                                                                                       | Lambda/API env or SSM         | If missing, suffix-only staging domains may be rejected; over-broad suffixes weaken domain binding.                                                                                                                                                |
| `AUTH_SIGNATURE_AUDIENCE`                | Backend API                          | Optional                             | Optional                                                           | Expected structured signature audience                                                                                                         | Service/API default                        | Staging API audience URI/string                                                                                                            | Lambda/API env                | If set incorrectly, valid signatures are rejected; if unset, default audience behavior is used.                                                                                                                                                    |
| `AUTH_SIGNATURE_ALLOWED_AUDIENCES`       | Backend API                          | Optional                             | Optional                                                           | Additional accepted structured-signature audiences for rollout                                                                                 | None/default only                          | Comma-separated accepted staging audiences                                                                                                 | Lambda/API env                | If not set, only default/primary audience is accepted; useful during client rollout but over-broad lists weaken binding.                                                                                                                           |
| `AUTH_LEGACY_WS_QUERY_TOKEN_ENABLED`     | Backend API websocket transport      | Compatibility flag                   | Optional; keep enabled during rollout                              | Allows legacy websocket query-token auth transport independently of structured signature enforcement                                           | Enabled unless explicitly `false`          | `true` or unset until all websocket clients use the new path                                                                               | Lambda/API env                | This is separate from `AUTH_STRUCTURED_SIGNATURES_REQUIRED`: it controls websocket token transport compatibility, not wallet signature format. Disabling early can break older websocket clients even if structured signatures are otherwise fine. |

Backend-only deployment note: deploying this backend first is compatible with the current web app. Old web clients will ignore the new `/api/settings.auth` object. Leave `SESSION_V2_MIGRATION_DEADLINE` unset until the FE PR is deployed and the migration prompt should begin. Before enabling FE v2 web-cookie auth, set `AUTH_WEB_CREDENTIAL_ORIGINS` to the web origin for that environment.

## Validation

- Local targeted format: `prettier --write docs/auth/README.md docs/auth/wallet-auth.md docs/auth/session-v2-migration-plan.md src/api-serverless/src/api-constants.test.ts src/api-serverless/src/api-constants.ts src/api-serverless/src/app.ts src/api-serverless/src/auth/auth-db.test.ts src/api-serverless/src/auth/auth.db.ts src/api-serverless/src/auth/auth.routes.ts src/entities/IRefreshToken.ts` passed; `.env.sample` files were inspected as plain text because Prettier cannot infer a parser for them.
- Local focused Jest without container global setup/per-test DB hooks: `jest src/api-serverless/src/auth/auth-db.test.ts src/api-serverless/src/api-constants.test.ts --runInBand --config <no globalSetup/globalTeardown/setupFilesAfterEnv>` passed (`2` suites, `12` tests).
- Local backend lint after these changes: `npm run lint` passed.
- Local whitespace check: `git diff --check` passed.
- Local backend `npm run tsc -- -p tsconfig.json --noEmit` no longer reports auth/CORS errors after the CORS delegate typing fix; it still fails on existing branch-wide profile-CMS/zod type errors outside this auth change.
- Local default `npm test -- src/api-serverless/src/auth/auth-db.test.ts src/api-serverless/src/api-constants.test.ts --runInBand` was not usable in this environment because Jest global setup requires a Testcontainers-compatible container runtime.
- CI before this report update: DCO, CodeRabbit, SonarCloud, and Snyk had passed; checks should be reviewed again after publishing this follow-up.
- SonarCloud leak query before this update: zero open/confirmed new-code issues.
- Review threads before this update: no unresolved actionable backend review threads found.

## Staging Deployment Summary

- Deployed API: `None`
- Deployed Lambdas/services: `None`
- Not deployed: `dbMigrationsLoop` and `api`; user explicitly requested a deployment plan only and no staging deploy.
- Deployment order executed: `None`
- GitHub Actions run links or run IDs: `None`

Planned staging order when approved:

1. Merge backend feature into the approved staging branch.
2. Configure backend env for a backend-first/silent rollout:
   - set `AUTH_SESSION_HASH_SECRET` to a dedicated high-entropy secret;
   - set `AUTH_WEB_CREDENTIAL_ORIGINS=https://staging.6529.io` for staging or `https://6529.io` for production before FE v2 cookie auth is enabled;
   - keep `AUTH_WALLET_CHAIN_ID=1` or unset unless intentionally testing another chain;
   - keep `AUTH_STRUCTURED_SIGNATURES_REQUIRED=false`;
   - keep `SESSION_V2_MIGRATION_DEADLINE` unset;
   - keep `AUTH_LEGACY_WS_QUERY_TOKEN_ENABLED=true` or unset;
   - keep `AUTH_LEGACY_REFRESH_DISABLED=false` or unset;
   - leave `AUTH_CONNECTION_SHARING_DISABLED` unset/`false` unless intentionally disabling connection sharing.
3. Deploy `dbMigrationsLoop` first for entity/schema synchronization.
4. Deploy `api`.
5. Smoke test backend auth/session/settings/connection-transfer endpoints.
6. After FE is deployed, set `SESSION_V2_MIGRATION_DEADLINE=<ISO datetime with timezone>` to start v1-auth migration prompts.
7. Only after all clients are verified, consider setting `AUTH_STRUCTURED_SIGNATURES_REQUIRED=true` to block new legacy/unstructured signing.
8. After the migration grace period and external-client communication are complete, set `AUTH_LEGACY_REFRESH_DISABLED=true` to stop legacy refresh tokens. Roll back by unsetting it or setting `false`.

The detailed rollout and rollback checklist is maintained in `docs/auth/session-v2-migration-plan.md`.

## Smoke-Test Plan

1. Request a structured nonce and complete web wallet session login/refresh/logout against staging.
2. Create and redeem a connection transfer code from web to native/mobile client.
3. Verify websocket authenticate/re-authenticate behavior, including expired-token rejection.

## Release Notes

- Adds wallet auth session v2 backend support for web/native session management and connection sharing.
- Tightens structured wallet signature verification and replay/domain/audience controls.
- Requires coordinated backend env rollout and frontend/client structured-signature compatibility before strict mode is enabled.
- Backend `/api/settings.auth` can be deployed before web; web migration behavior starts only after the FE that reads it is deployed and a backend deadline or strict flag is set.

## Residual Risks And Follow-Ups

- Report commit may retrigger CI/bots; latest PR checks should be reviewed after this file is published.
- Backend Testcontainers-backed tests were not executable locally in this environment.
- Branch-wide backend typecheck currently has pre-existing profile-CMS/zod errors outside the auth change; this follow-up removed the auth/CORS type error introduced by the credentialed CORS delegate.
- `AUTH_STRUCTURED_SIGNATURES_REQUIRED=true` should be delayed until all clients are confirmed to send structured signatures where required.
- `AUTH_LEGACY_REFRESH_DISABLED=true` should be delayed until refresh-token traffic and external API clients are understood; it is the hard v1-refresh cutoff.
