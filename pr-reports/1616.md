# PR Report: Wallet auth session v2 APIs and transfer codes

- GitHub PR: https://github.com/6529-Collections/6529seize-backend/pull/1616
- Branch: `codex/wallet-security-hardening`
- Target: `main`
- Related frontend PR: https://github.com/6529-Collections/6529seize-frontend/pull/2556

## Task Summary

Auth v2 backend hardening for wallet sessions, structured wallet signatures, connection sharing, and websocket authentication compatibility.

## Backend Change Summary

- Added/updated auth session v2 API behavior for web/native wallet sessions, refresh/logout, nonce/signature handling, and connection transfer/redeem flows.
- Added database-backed wallet auth session and connection transfer persistence.
- Hardened structured wallet signature verification, replay protection, allowed-domain/audience checks, and websocket authentication expiry handling.
- Updated OpenAPI/auth contract surfaces and generated API artifacts where applicable.
- Added and iterated focused auth/session/signature tests.

## Backend Impact Summary

- New Lambdas/services created: `None`
- Existing Lambdas/services changed: `api`
- Existing Lambdas/services redeployed due to shared-code impact: `api`
- API changes: auth session v2 routes, nonce/signature behavior, connection transfer/redeem behavior, websocket auth handling
- DB/entity/migration changes: wallet auth session and wallet connection transfer persistence/entity sync
- Queue/event/media/edge/integration changes: `None`
- Backend docs/architecture docs: no public architecture-doc update was identified as required for this report-only update

## Env And Config

| Name | Owner | Status | Requiredness | Purpose | Default/Fallback | Recommended staging value shape | Location | Rollout / failure notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `AUTH_CONNECTION_SHARING_ENABLED` | Backend API | New/pre-existing for feature | Required to enable connection sharing | Gates web-to-native connection transfer endpoints | Disabled when unset/false | `true` for staging feature validation | Lambda/API env or SSM | If false, connection sharing flows remain unavailable. |
| `AUTH_SESSION_HASH_SECRET` | Backend API | New/recommended | Strongly recommended secret | Hashes session/refresh secrets independently | Falls back to existing JWT secret if unset | Dedicated high-entropy secret | Secret manager/SSM/Lambda env | If weak/missing, secret separation is reduced. Do not expose value. |
| `AUTH_CONNECTION_SHARE_CODE_TTL_SECONDS` | Backend API | Optional | Optional | Controls connection transfer code lifetime | Default approximately 300 seconds | Positive integer, normally `300` | Lambda/API env | Too low causes mobile handoff expiry; too high increases code exposure window. |
| `AUTH_STRUCTURED_SIGNATURES_REQUIRED` | Backend API | Optional rollout gate | Optional initially | Rejects legacy/unstructured signatures where structured signatures are expected | `false` | `false` until frontend/mobile clients emit structured messages everywhere; later `true` | Lambda/API env | Turning on too early breaks clients still sending legacy/unstructured messages. |
| `AUTH_SIGNATURE_ALLOWED_DOMAINS` | Backend API | Optional/required by domain coverage | Required if staging domains are not already covered | Explicit allowed signing domains | Repo/service default list | Comma-separated first-party staging domains | Lambda/API env or SSM | Incorrect values reject valid signatures or allow unintended domains. |
| `AUTH_SIGNATURE_ALLOWED_DOMAIN_SUFFIXES` | Backend API | Optional/required by domain coverage | Required if suffix coverage is needed | Allows first-party domain suffixes | Repo/service default list | Comma-separated first-party suffixes | Lambda/API env or SSM | Over-broad suffixes weaken domain binding. |
| `AUTH_SIGNATURE_AUDIENCE` | Backend API | Optional | Optional | Expected signature audience | Service/API default | Staging API audience URI/string | Lambda/API env | Mismatch rejects signatures. |
| `AUTH_SIGNATURE_ALLOWED_AUDIENCES` | Backend API | Optional | Optional | Additional accepted audiences for rollout | None/default only | Comma-separated accepted staging audiences | Lambda/API env | Useful during client rollout; over-broad list weakens binding. |
| `AUTH_LEGACY_WS_QUERY_TOKEN_ENABLED` | Backend API | Compatibility flag | Optional | Allows legacy websocket query-token compatibility | Enabled/compatible unless disabled | Keep enabled for staged rollout unless all clients use new path | Lambda/API env | Disabling early can break older websocket clients. |

## Validation

- Local: `npm run lint` passed after syncing with current `origin/main`.
- Local focused Jest: auth-focused Jest could not start in this environment because Testcontainers could not find a container runtime.
- CI before this report commit: DCO, CodeRabbit, SonarCloud, and Snyk passed; `Build backend and API` was still in progress at the last refresh.
- SonarCloud leak query: zero open/confirmed new-code issues.
- Review threads: no unresolved actionable backend review threads found.

## Staging Deployment Summary

- Deployed API: `None`
- Deployed Lambdas/services: `None`
- Not deployed: `dbMigrationsLoop` and `api`; user explicitly requested a deployment plan only and no staging deploy.
- Deployment order executed: `None`
- GitHub Actions run links or run IDs: `None`

Planned staging order when approved:

1. Merge backend feature into the approved staging branch.
2. Deploy `dbMigrationsLoop` first for entity/schema synchronization.
3. Deploy `api`.
4. Smoke test backend auth/session/connection-transfer endpoints before frontend staging validation.

## Smoke-Test Plan

1. Request a structured nonce and complete web wallet session login/refresh/logout against staging.
2. Create and redeem a connection transfer code from web to native/mobile client.
3. Verify websocket authenticate/re-authenticate behavior, including expired-token rejection.

## Release Notes

- Adds wallet auth session v2 backend support for web/native session management and connection sharing.
- Tightens structured wallet signature verification and replay/domain/audience controls.
- Requires coordinated backend env rollout and frontend/client structured-signature compatibility before strict mode is enabled.

## Residual Risks And Follow-Ups

- Report commit may retrigger CI/bots; latest PR checks should be reviewed after this file is published.
- Backend Testcontainers-backed tests were not executable locally in this environment.
- `AUTH_STRUCTURED_SIGNATURES_REQUIRED=true` should be delayed until all clients are confirmed to send structured signatures where required.
