#!/usr/bin/env bash

set -euo pipefail

echo "Direct npx usage is blocked in this repository." >&2
echo "Use the \`6529\` wrapper instead." >&2
echo "Examples:" >&2
echo "  6529 exec <bin> [args...]" >&2
echo "  6529 run <script>" >&2
exit 1
